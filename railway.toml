[deploy]
startCommand = "chmod 664 /app/crm/crm.db 2>/dev/null; rm -f /app/crm/crm.db-shm /app/crm/crm.db-wal; exec gunicorn --chdir crm app:app --workers 1 --threads 8 --timeout 120 --bind 0.0.0.0:$PORT"
